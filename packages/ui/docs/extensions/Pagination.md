# Extension specification: `Pagination`

Pagination gives paged and cursor result sets a labelled navigation landmark. It is entirely
controlled: the package renders links and states, and the consumer owns page number, URL, fetching,
and total-count logic. Compose it with content, item, link, previous, next, and ellipsis pieces.
