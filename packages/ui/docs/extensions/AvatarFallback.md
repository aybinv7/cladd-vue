# Extension specification: `AvatarFallback`

Initials, an icon, or any consumer content shown while `AvatarImage` has no `src`, is loading, or
has failed. Always mounted at the container's full size; see `Avatar.md`.
