# Extension specification: `Empty`

Empty is the shared shape for a no-data, no-results, or first-run state. Compose it from
`EmptyHeader` (wrapping `EmptyMedia`, `EmptyTitle`, `EmptyDescription`) and `EmptyContent` for
actions. The package supplies no default icon, illustration, or copy — every instance is
consumer-authored so no generic component carries product-specific text.
