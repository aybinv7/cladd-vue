# Extension specification: `PaginationEllipsis`

Presentation-only truncation marker between page links. Hidden from assistive technology with
`aria-hidden="true"`; it is never a focusable or actionable control.
