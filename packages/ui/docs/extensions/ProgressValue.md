# Extension specification: `ProgressValue`

Displays `Progress`'s current value, formatted as a rounded percentage by default. Renders nothing
while indeterminate unless the consumer supplies its own slot content (for example, "Loading…").
