# Extension specification: `Breadcrumb`

Breadcrumb gives application routes a semantic `nav` and ordered trail. Compose it with item, link,
separator, page, and ellipsis pieces; the current page exposes `aria-current="page"`.
