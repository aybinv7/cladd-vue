# Port manifest: `Button`

- Upstream commit: `fadd8efe935111f31d7c933238db5ce5d3a55d71` (`@cladd-ui/react` 0.18.5)
- Upstream files: `src/components/Button.tsx`, `src/components/FocusRing.tsx`,
  `src/components/SurfaceCut.tsx`, `src/shared/size-utls.ts`, `src/shared/rounded-classes.ts`,
  `src/styles/radius.css`, `src/styles/spacing.css`, `src/styles/font-size.css`
- Vue files: `src/components/actions/Button.vue`, `src/components/actions/button.contracts.ts`,
  `src/components/feedback/FocusRing.vue`, `src/components/feedback/Spinner.vue`,
  `src/styles/controls.css`, `src/styles/tokens.css`
- Diffed: 2026-08-03, upstream read from `reference/cladd/`

Read upstream paths below as `reference/cladd/<path>`. Vue paths are relative to `packages/ui/`.

## Props

| Upstream prop                           | Upstream ref                                     | Vue surface                                                | Vue ref                                         | Default               | Verdict    | Note                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------- | ------------------------------------------------ | ---------------------------------------------------------- | ----------------------------------------------- | --------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `children`                              | `Button.tsx:25`                                  | default slot                                               | `Button.vue:68,142`                             | —                     | transposed |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `className`                             | `Button.tsx:27`, default `112`                   | `class` through `$attrs`                                   | `Button.vue:16,87,103`                          | `''` → none           | transposed |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `disabled`                              | `Button.tsx:29`, default `113`                   | `disabled`                                                 | `Button.vue:25,48,111`                          | `false`               | deviated   | Upstream renders only `pointer-events-none` (`Button.tsx:202`), `data-disabled`, `tabIndex={-1}`, and `opacity-40` on the content. Vue adds native `disabled` when `as="button"` (`Button.vue:119`), `aria-disabled` otherwise (`Button.vue:114`), and a capture-phase activation guard (`Button.vue:90`–`97`, `121`). Accessibility hardening beyond upstream; the extra attributes and the swallowed click are observable.                  |
| `readOnly`                              | `Button.tsx:31`                                  | `readOnly`                                                 | `Button.vue:33,56,118`                          | `undefined` → `false` | transposed | All upstream reads (`Button.tsx:203`, `221`, `224`–`226`, `249`) treat `undefined` and `false` identically.                                                                                                                                                                                                                                                                                                                                   |
| `rounded`                               | `Button.tsx:36`, default `115`                   | `rounded`                                                  | `Button.vue:34,57,107`                          | `false`               | ported     |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `size`                                  | `Button.tsx:38`, default `116`, union `20`       | `size`                                                     | `Button.vue:35,58,106`, `contracts.ts:1`        | `'md'`                | ported     | Seven tokens, same order.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `as`                                    | `Button.tsx:41`, default `117`                   | `as`                                                       | `Button.vue:21,44`                              | `'button'`            | transposed | Forwarded to the surface's polymorphic root as at `Button.tsx:187`.                                                                                                                                                                                                                                                                                                                                                                           |
| `variant`                               | `Button.tsx:43`, default `121`                   | `variant`                                                  | `Button.vue:40,63,84`                           | `'gradient'`          | ported     | See `surfaceLevel` for the `surface="cut"` forwarding difference.                                                                                                                                                                                                                                                                                                                                                                             |
| `color`                                 | `Button.tsx:45`, default `118`                   | `color`                                                    | `Button.vue:23,46,77,115`                       | `''` → `undefined`    | deviated   | Upstream applies the primary text colour only when `color && color !== 'neutral'` and applies `text-cladd-on-primary` for `neutral` only in fill variants (`Button.tsx:194`–`197`). Vue sets `data-cladd-explicit-accent` for _any_ explicit accent (`Button.vue:115`) and `controls.css:259`–`261` then colours the label with `--cladd-primary`, so `color="neutral"` on a non-fill variant renders a different label colour than upstream. |
| `outline`                               | `Button.tsx:47`, default `119`                   | `outline`                                                  | `Button.vue:31,54,81`                           | `true`                | ported     |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `contentClassName`                      | `Button.tsx:49`, default `120`                   | `contentClassName`                                         | `Button.vue:24,47,79`                           | `''` → `undefined`    | ported     | Forwarded to the surface content wrapper as at `Button.tsx:210`.                                                                                                                                                                                                                                                                                                                                                                              |
| `style`                                 | `Button.tsx:51`, forwarded `239`                 | `style` through `$attrs`                                   | `Button.vue:16,87`                              | —                     | transposed |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `focused`                               | `Button.tsx:53`, default `123`                   | `focused`                                                  | `Button.vue:27,50,133,135`                      | `false`               | ported     | `focused \|\| (focusable && !readOnly && !disabled)` (`Button.tsx:249`) reproduced at `Button.vue:133`; `force={focused}` (`Button.tsx:251`) at `Button.vue:135`.                                                                                                                                                                                                                                                                             |
| `tightFocusRing`                        | `Button.tsx:55`, default `124`                   | `tightFocusRing`                                           | `Button.vue:39,62,138`                          | `false`               | ported     | `offset={!tightFocusRing}` (`Button.tsx:252`) reproduced.                                                                                                                                                                                                                                                                                                                                                                                     |
| `pressed`                               | `Button.tsx:57`                                  | `pressed` forwarded to the surface                         | `Button.vue:32,55,82`                           | `undefined` → `false` | deviated   | The forwarding matches `Button.tsx:220`, but `data-pressed` (`Button.tsx:190`) is not emitted. The audit records the `data-*` set as matching; it does not.                                                                                                                                                                                                                                                                                   |
| `multiline`                             | `Button.tsx:59`                                  | `multiline`                                                | `Button.vue:30,53,108,137`                      | `undefined` → `false` | transposed | Height-to-`min-height` swap (`Button.tsx:140`–`142`) and the multiline pill radii become `controls.css:182`–`241`.                                                                                                                                                                                                                                                                                                                            |
| `square`                                | `Button.tsx:61`                                  | `square`                                                   | `Button.vue:36,59,109`                          | `undefined` → `false` | transposed | `aspect-square` plus dropped inline padding (`Button.tsx:201`, `216`) become `controls.css:243`–`245`, `288`–`291`.                                                                                                                                                                                                                                                                                                                           |
| `surface`                               | `Button.tsx:68`, default `128`, union `21`       | `surface`                                                  | `Button.vue:37,60,73`, `button.contracts.ts:13` | `'surface'`           | ported     | `'cut'` selects `SurfaceCut` as at `Button.tsx:171`.                                                                                                                                                                                                                                                                                                                                                                                          |
| `clickable`                             | `Button.tsx:70`, default `129`                   | `clickable`                                                | `Button.vue:22,45,76`                           | `true`                | ported     | `clickable && !disabled && !readOnly` (`Button.tsx:224`) reproduced exactly.                                                                                                                                                                                                                                                                                                                                                                  |
| `hoverable`                             | `Button.tsx:72`, default `130`                   | `hoverable`                                                | `Button.vue:28,51,80`                           | `true`                | ported     | `hoverable && !disabled && !readOnly` (`Button.tsx:225`) reproduced exactly.                                                                                                                                                                                                                                                                                                                                                                  |
| `focusable`                             | `Button.tsx:74`, default `131`                   | `focusable`                                                | `Button.vue:26,49,133`                          | `true`                | ported     |                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `surfaceLevel`                          | `Button.tsx:76`, forwarded `227`                 | `surfaceLevel` forwarded as `level`                        | `Button.vue:38,61,84`                           | `undefined`           | deviated   | Upstream forwards `level` and `variant` unconditionally, including to `SurfaceCut`, which declares neither (`SurfaceCut.tsx:10`–`51`), so they land in `...rest` and reach the DOM. Vue forwards both only when `surface="surface"` (`Button.vue:83`–`85`), so `surface="cut"` silently ignores `surfaceLevel` and `variant` instead of emitting stray attributes.                                                                            |
| `loading`                               | `Button.tsx:79`                                  | `loading`                                                  | `Button.vue:29,52,113,117,126`                  | `undefined` → `false` | transposed | Spinner overlay, `data-loading`, and the content fade-out all reproduced; Vue additionally sets `aria-busy` (`Button.vue:113`).                                                                                                                                                                                                                                                                                                               |
| `ref`                                   | `Button.tsx:81`, merge `229`–`236`               | root element via the component's template ref              | `Button.vue:101`                                | —                     | transposed | Upstream's manual ref-merge closure is unnecessary in Vue.                                                                                                                                                                                                                                                                                                                                                                                    |
| `onContextMenuCapture`                  | `Button.tsx:237`                                 | `@contextmenu.prevent`                                     | `Button.vue:122`                                | —                     | transposed | Upstream cancels in the capture phase; Vue cancels in the bubble phase. Same suppression, later in propagation.                                                                                                                                                                                                                                                                                                                               |
| `useComponentDefaults('Button', props)` | `Button.tsx:136`; `ButtonDefaultProps` `92`–`94` | —                                                          | —                                               | —                     | deviated   | Provider-supplied per-component defaults are not ported. Audit finding 7.                                                                                                                                                                                                                                                                                                                                                                     |
| —                                       | —                                                | `accent`                                                   | `Button.vue:20,43,115,128,134`                  | `undefined`           | deviated   | Vue-only prop; a second spelling of `color` threaded into `Spinner` and `FocusRing`. Widens the public API beyond upstream.                                                                                                                                                                                                                                                                                                                   |
| —                                       | —                                                | `aria-busy`, `aria-disabled`, `data-cladd-explicit-accent` | `Button.vue:113`–`115`                          | —                     | deviated   | Vue-only attributes. Upstream's attribute set is `data-disabled`, `data-readonly`, `data-pressed`, `data-loading` (`Button.tsx:188`–`191`).                                                                                                                                                                                                                                                                                                   |

## Copied literals

| Value                                                                         | Upstream ref                                                         | Vue ref                                               |
| ----------------------------------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------- |
| Spinner size map `2xs→2xs, xs→2xs, sm→xs, md→sm, lg→md, xl→lg, 2xl→xl`        | `Button.tsx:175`–`183`                                               | `button.contracts.ts:3`–`11`                          |
| Icon sizes `12px` for `2xs`/`xs`, `16px` otherwise (`size-3` / `size-4`)      | `Button.tsx:96`–`104`                                                | `controls.css:293`–`307`                              |
| Inline padding `10px` (`px-2.5`), `14px` for `2xl` (`px-3.5`)                 | `Button.tsx:150`–`158`                                               | `controls.css:270`, `283`–`286`                       |
| Block padding `0` for `2xs`, `2px` for `xs`, `4px` otherwise                  | `Button.tsx:212`                                                     | `controls.css:270`, `273`–`281`                       |
| Content gap `8px` (`gap-2`)                                                   | `Button.tsx:211`                                                     | `controls.css:269`                                    |
| Font size `12px` for every size (`text-cladd-xs`)                             | `Button.tsx:159`–`167`, `font-size.css:4`                            | `controls.css:142`, `tokens.css:87`                   |
| Font weight `600` (`font-semibold`)                                           | `Button.tsx:193`                                                     | `controls.css:144`                                    |
| `inline-block`, `text-left`, `outline-0`, `select-none`                       | `Button.tsx:193`                                                     | `controls.css:135`, `141`, `148`, `149`               |
| Disabled content `opacity-40`                                                 | `Button.tsx:215`                                                     | `controls.css:310`                                    |
| Loading content `scale-0 opacity-0!`                                          | `Button.tsx:217`                                                     | `controls.css:315`–`316`                              |
| Spinner overlay `absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2` | `Button.tsx:246`                                                     | `controls.css:320`–`323`                              |
| Spinner entry `starting:scale-0 starting:opacity-0`, `duration-200`           | `Button.tsx:246`                                                     | `controls.css:325`–`335` (`@starting-style`)          |
| Cursor `pointer` only for `a` or an `href`, otherwise `auto`                  | `Button.tsx:203`–`205`                                               | `controls.css:251`–`257`                              |
| Root heights = `h-cladd-{size}` ladder                                        | `Button.tsx:142`, `size-utls.ts:27`–`36`, `spacing.css:3`–`9`        | `controls.css:135`, `152`–`180`, `tokens.css:34`–`40` |
| Multiline `min-h-cladd-{size}` ladder                                         | `Button.tsx:141`, `size-utls.ts:49`–`58`                             | `controls.css:182`–`209`                              |
| Item radii = `rounded-cladd-{size}`                                           | `rounded-classes.ts:20`–`27`, `radius.css:7`–`14`                    | `controls.css:140`, `152`–`180`, `tokens.css:51`–`59` |
| `rounded` = `rounded-full`                                                    | `rounded-classes.ts:18`                                              | `controls.css:211`–`213` (`9999px`)                   |
| `rounded` + `multiline` = `rounded-cladd-full-{size}`                         | `rounded-classes.ts:9`–`16`, `radius.css:16`–`22`                    | `controls.css:215`–`241`, `tokens.css:60`–`66`        |
| Focus radii = `rounded-cladd-focus-{size}`                                    | `Button.tsx:255`, `rounded-classes.ts:42`–`49`, `radius.css:40`–`46` | `controls.css:52`, `68`–`90`, `tokens.css:67`–`73`    |
| Focus radii, rounded + multiline = `rounded-cladd-focus-full-{size}`          | `rounded-classes.ts:31`–`38`, `radius.css:48`–`54`                   | `controls.css:96`–`122`, `tokens.css:74`–`80`         |
| Tight focus ring = `rounded-[inherit]`                                        | `Button.tsx:255`                                                     | `controls.css:64`–`66`, `124`–`126`                   |
| Focus ring `border-2`, `scale-95`, `opacity-0`, `duration-200`                | `FocusRing.tsx:59`                                                   | `controls.css:51`, `53`–`57`                          |
| Focus ring offset `-inset-1.5` = `-6px`, flush = `inset-0`                    | `FocusRing.tsx:60`                                                   | `controls.css:48`, `60`–`62`                          |
| Focus ring reveal on `group-focus-visible/cladd-button`                       | `FocusRing.tsx:41`                                                   | `controls.css:128`–`132`                              |
| Spinner rotation `1.5s infinite linear`                                       | `styles/spinner.css`                                                 | `motion.css:21`–`33`                                  |

## Deviations

- `data-pressed` not emitted (`Button.tsx:190`) — the pressed state reaches the surface but not the
  attribute, so consumer styling and tests keyed on `data-pressed` break. Contradicts the audit's
  "`data-*` attribute set match" claim for `Button`.
- `data-cladd-explicit-accent` (`Button.vue:115`) replaces upstream's `color !== 'neutral'` condition
  (`Button.tsx:194`–`197`), changing the label colour for `color="neutral"` on non-fill variants.
- `aria-busy`, `aria-disabled`, native `disabled`, and the capture-phase activation guard
  (`Button.vue:90`–`97`, `113`–`121`) are Vue additions with no upstream counterpart.
- `accent` prop (`Button.vue:20`) — Vue-only alias for `color`.
- `surfaceLevel` and `variant` are withheld from `SurfaceCut` (`Button.vue:83`–`85`) where upstream
  forwards them into `...rest` (`Button.tsx:222`, `227`). Upstream's behaviour leaks unknown
  attributes onto the DOM because `SurfaceCut` declares neither prop; the Vue behaviour drops them
  instead. Divergent either way, and undocumented until now.
- `useComponentDefaults` / `ButtonDefaultProps` not ported (`Button.tsx:136`, `92`–`94`). Audit
  finding 7.
- `contextmenu` suppression moved from capture to bubble phase (`Button.tsx:237` vs
  `Button.vue:122`).
- `controls.css:137`–`147` adds `appearance: none`, `margin: 0`, `border: 0`, `font-family: inherit`,
  `line-height: 1.5`, `background: transparent`, and `padding: 0`. Upstream gets all but the
  line-height from Tailwind's preflight, so those are reset equivalents rather than copied values.
  `line-height: 1.5` (`controls.css:145`) has no upstream source and is a Vue choice.
- `paddings` and `fontSizes` (`Button.tsx:150`–`167`) are declared inline upstream and were resolved
  straight into CSS. They are not in `button.contracts.ts`, so no value-lock test can import them.
  Bucket-A extraction gap, audit process fix 3.

## Verification

Compared prop-by-prop against `Button.tsx`, then every Tailwind utility string in the render against
`controls.css`. The spinner size map, icon size map, padding map, and font-size map were compared
entry by entry. Radius and height ladders were followed through `rounded-classes.ts` and
`size-utls.ts` into `radius.css` / `spacing.css` and then into `tokens.css`. `SurfaceCut.tsx` was
read to confirm it declares no `level` or `variant`.

Not verified here: `Spinner` and `FocusRing` beyond the values `Button` depends on, `SurfaceCut`
composition, and `styles/colors.css`. Each needs its own manifest.

## Resolved since this manifest was written

Fixed 2026-08-03, verified against `reference/cladd/src/components/Button.tsx` and locked by tests in
`packages/ui/tests/actions.test.ts`:

- `data-pressed` is emitted again (`Button.tsx:190`).
- The accent text hook is scoped to non-neutral explicit accents, matching `Button.tsx:194`-`197`. The
  fill branch needs no hook: `.cladd-surface--fill` already sets `--cladd-on-primary` for every fill
  variant, which is what upstream expresses through `text-cladd-on-primary`.
- `contextmenu` preventDefault runs on the capture phase again (`Button.tsx:237`).

Still open: `useComponentDefaults` is absent package-wide, and `surfaceLevel` / `variant` are withheld
from `SurfaceCut` where upstream forwards them through `...rest`. Upstream leaks both to the DOM because
`SurfaceCut.tsx` declares neither, so the port's behaviour is arguably better; it stays a registered
deviation rather than a defect.
